spring:
  servlet:
    multipart:
      enabled: true
      max-file-size: 10MB
      max-request-size: 10MB

server:
  port: 0

eureka:
  instance:
    prefer-ip-address: true
    instanceId: ${spring.application.name}:${vcap.application.instance_id:${spring.application.instance_id:${random.value}}}

gcp:
  project-id: dilsara
  credentials-location: dilsara-0247aef3a371.json
  bucket-id: eca-lm-project-bucket
